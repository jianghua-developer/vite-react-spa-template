# 公共环境变量（所有 mode 生效）

# 运行时配置：VITE_APP_CONFIG_* 前缀变量会自动挂载到 window.__APP_CONFIG__，
# 挂载键 = 去掉 VITE_APP_CONFIG_ 前缀后的原名（如 API_BASE_URL / TIMEOUT），
# 可由 public/config.js 运行时覆盖（无需重新打包），经 getAppConfigValue() 读取
VITE_APP_CONFIG_API_BASE_URL={{apiBaseUrl}}
VITE_APP_CONFIG_TIMEOUT=15000
